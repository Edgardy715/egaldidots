# ── Editor / OS cruft ──
.DS_Store
*~
*.swp
*.swo
*.tmp

# ── GNU Stow ──
.stow-recovery

# ── Backups locales (install.sh los escribe fuera del repo; por las dudas) ──
*.bak
*.orig

# Note: ~/Wallpapers IS tracked (an explicit part of the repo, by design).
